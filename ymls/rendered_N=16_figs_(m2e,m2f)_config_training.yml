command:
- python
- -u
- training.py
- --num_timesteps
- '2000000'
- --batch_size
- '1024'
- --log_wandb
- --exp_name
- sweep_all_2D
- --normalize_dataset
- ${normalize_dataset}
- --use_ln
- '0'
- --linear_network
- ${linear_network}
- --use_bias
- --fits_in_vram
- --group_name
- rep_sweep
- --repr_fn
- ${repr_fn}
- --repr_dim
- ${repr_dim}
- --h_dim
- '256'
- --num_layers
- ${num_layers}
- --seed
- ${seed}
- --contrastive_loss_fn
- ${contrastive_loss_fn}
- --data_folder_to_analyze
- ${data_folder_to_analyze}
- --datasets_to_analyze
- ${datasets_to_analyze}
- --energy_fn
- ${energy_fn}
- --precision
- ${precision}
- --lr
- ${lr}
- --patience
- ${patience}
- --frac_of_data
- ${frac_of_data}
- --image_normalize
method: grid
parameters:
  contrastive_loss_fn:
    values:
    - forward_backward
  data_folder_to_analyze:
    values:
    - /n/fs/rl-cj7280/epr_estimator/datasets/figures_dataset_dump/rendered_nbeads_N=16_more_sep
  energy_fn:
    values:
    - dot
  frac_of_data:
    values:
    - 1.0
  linear_network:
    values:
    - true
    - false
  lr:
    values:
    - 3.0e-05
  normalize_dataset:
    values:
    - none
  num_layers:
    values:
    - 2
  patience:
    values:
    - 10
  precision:
    values:
    - highest
  repr_dim:
    values:
    - 1
    - 2
    - 4
    - 8
    - 16
  repr_fn:
    values:
    - linear_local_subspaces
  seed:
    values:
    - 1
program: training.py
